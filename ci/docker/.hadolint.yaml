failure-threshold: warning
format: json
ignored:
  - DL3003 # Use WORKDIR instead of 'cd' (suppressed to avoid false positives within RUN chains)
  - DL3049 # Add LABEL instructions
label-schema:
  author: text
  contact: email
  created: rfc3339
  documentation: url
  git-revision: hash
  license: spdx
  version: semver
no-fail: false
strict-labels: false
