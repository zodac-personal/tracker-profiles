## Added progress bar

To make tracking execution easier, I've added a progress bar to the bottom of the console output using the [Clique](https://github.com/kusoroadeolu/Clique/) library. See the new `PROGRESS_BAR_*` environment variables for configuration options (or to disable).
