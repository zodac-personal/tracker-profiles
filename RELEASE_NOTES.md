## Auto-Continue Manual Inputs

For trackers with manual inputs, if performing this action reloads the screen (Captcha, Cloudflare, etc.), then the user input pop-up will automatically close and execution will continue. This might save some clicks.
